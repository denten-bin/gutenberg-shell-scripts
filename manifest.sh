#!/bin/sh

# This script will iterate over all files in a directory to create a manifest
# of documents using the gutenberg.org meta data fields. These include file
# name, title, author, language, and encoding. The output is in .csv format
